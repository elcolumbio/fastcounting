# copy this file into the one specified in helper
# path_to_config = pathlib.Path(f'{homedir}/.config/fastcounting.yaml')

Redis:
  host: localhost
  port: 6379
  password: Null

# specify exactly one of those two
rel_to_home_datafolder: Nextcloud/data/finance/
absolut_datafolder: Null
